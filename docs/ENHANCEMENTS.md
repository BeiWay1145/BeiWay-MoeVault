# BeiWay-MoeVault 功能增强与补全待办清单

> 基于现有功能梳理的不足与增强建议，按优先级分组。
> 状态图例：⬜ 未开始 / 🚧 进行中 / ✅ 已完成

---

## P0 — 影响日常使用的缺口

### 1. 打标模型种类选择（本次已做）
- [x] 推理服务抽象多探测器：cl-tagger（SIGLIP2 ONNX）保留
- [x] wd14 tagger（ONNX + selected_tags.csv）加载/推理
- [x] `auto` 目录内容自动判定种类（model_vocabulary.json → cl_tagger / selected_tags.csv → wd14）
- [x] 设置页「模型种类」下拉（auto / cl-tagger / WD14）
- [ ] **WD14 HF torch 版接入**（框架已预留：目录含模型仓库时走 transformers）
- [ ] wd14 模型文件就位后端到端验证（本机当前无 wd14 模型）

### 2. 桌面壳推理服务一键可用（本次已做）
- [x] 安装包内嵌推理服务代码（python/server 等资源）
- [x] 生产模式运行时目录改为用户数据目录（%LOCALAPPDATA%\BeiWay-MoeVault\python，绕开 Program Files 写权限）
- [x] 启动前关键依赖预检（fastapi/uvicorn/transformers），缺失明确报错
- [x] 设置页「一键安装依赖」（pip 装 CPU 包）+ 自动重启
- [x] 顶栏状态圆点 / 设置页状态卡片

### 3. 标签管理 API（原规划未完成项）
- [ ] `GET/POST/PUT/DELETE /api/v1/tags` 自定义标签 CRUD（TagManageView.vue 已有页面骨架）
- [ ] 标签合并（多标签 → 一）、标签重命名（级联更新 images_tags）
- [ ] 标签黑名单（打标/显示时过滤）
- [ ] 标签翻译字典管理（英文标签 → 中文显示）

### 4. 复制导入（当前仅移动导入）
- [ ] 导入方式支持 `copy`（保留源文件），TopBar 已提示"暂未支持"
- [ ] 导入失败重试 / 断点续传（大目录中断后跳过已入库）

---

## P1 — 筛选与浏览优化

### 5. 筛选增强（用户点名示例）
- [x] 搜索式筛选（danbooru 风格）：图库工具栏标签/状态联想搜索框 + 词频 chips 即时筛选（2026-08-28）
- [ ] 保存/命名筛选方案（当前筛选条件持久化到本地/LocalStorage 或后端预存；smart_views 表已建）
- [ ] 标签多选 OR 支持（当前 AND/排除；追加"任一命中"模式）
- [ ] 空结果友好引导（无匹配时给出可能原因）
- [ ] 浏览位置/筛选状态导出分享（URL query 带筛选参数）

### 6. 图库浏览
- [ ] 分类导航（废案暂缓）：画师/系列/角色分组浏览已移除 UI，待「图集」（真人写真/漫画独立集合）设计敲定后以独立路由登场；后端 /tags/browse 接口与 tag_covers 表保留备用
- [ ] 随机浏览模式（"洗牌"入口，当前有 random 排序但无持续随机浏览）
- [ ] 瀑布流虚拟滚动（大量图片性能）
- [ ] 网格模式按文件哈希/导入批次分组视图

### 7. 详情页与预览
- [ ] 相似图片推荐（同 pHash 组 / 同来源 URL）
- [ ] 打标来源展示（danbooru/gelbooru/local 标注已在数据层）
- [ ] 图片旋转/翻转元数据支持

---

## P2 — 后台任务与管道增强

### 8. 打标管道
- [ ] SauceNAO 结果缓存显隐控制（当前自动查缓存）
- [ ] 打标任务按来源跳过（如指定只用本地模型不打 SauceNAO）
- [ ] 批量打标进度细粒度（当前按张计）
- [ ] 失败重试策略可配置（重试次数/间隔）

### 9. 美学评分
- [ ] 评分历史曲线（同一图多次评分对比）
- [ ] 高分集合视图（评分 TOP N 快速浏览）

### 10. 任务中心
- [ ] 任务取消（当前部分支持，补全所有任务类型）
- [ ] 任务定时/续跑（断点继续未完成批次）
- [ ] 任务通知系统级（Windows 通知，当前仅应用内）

---

## P3 — 数据与可靠性

### 11. 备份与迁移
- [ ] 数据库 + 设置导出/导入（单文件打包）
- [ ] 模型目录配置导出（换机迁移一键还原）

### 12. 数据一致性
- [ ] library 目录增量重扫（外部文件增删同步元数据）
- [ ] 孤儿文件清理（文件存在但 DB 无记录）

### 13. 性能
- [ ] SQLite 查询计划优化（大数据量筛选索引）
- [ ] 缩略图二级缓存（内存 LRU）

---

## P4 — 桌面与体验

### 14. 桌面壳
- [ ] 自更新（tauri updater，需 bump 版本 + release 托管；用户当前明确不上传）
- [ ] 开机自启开关
- [ ] 安装向导/首启引导（模型放置提示、依赖安装进度条）

### 15. 设置
- [ ] 主题跟随系统（当前暗/亮手动）
- [ ] 快捷键（全屏、导入、搜索聚焦）

### 16. 统计与可视化
- [ ] 标签云页（按使用频率、来源分类）
- [ ] 图库总览图表（导入趋势、评分分布）

---

## 执行建议

1. P0-3（标签管理）与 P1-5（筛选增强）投入产出比最高，优先做
2. P1-6 标签分类导航依赖标签数据质量，建议在 P0-3 之后
3. P4 的桌面增强依赖版本管理决策（是否启用 updater），与 release 发布策略绑定